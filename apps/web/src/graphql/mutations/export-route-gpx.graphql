mutation ExportRouteGPX($routeId: ID!) {
  exportRouteGPX(routeId: $routeId) {
    ... on GPXExportSuccess {
      downloadUrl
      fileName
    }
    ... on GPXExportQuotaExceeded {
      message
      upgradeUrl
    }
  }
}
