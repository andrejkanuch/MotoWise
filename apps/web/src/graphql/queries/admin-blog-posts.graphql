query AdminBlogPosts($input: ListBlogPostsInput) {
  adminBlogPosts(input: $input) {
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      cursor
      node {
        ...BlogPostFields
      }
    }
  }
}
