mutation SubmitDiagnostic($input: SubmitDiagnosticInput!) {
  submitDiagnostic(input: $input) {
    id
    userId
    motorcycleId
    severity
    confidence
    relatedArticleId
    resultJson
    description
    photoUrl
    status
    createdAt
  }
}
