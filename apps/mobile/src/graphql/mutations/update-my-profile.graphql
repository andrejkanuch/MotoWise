mutation UpdateMyProfile($input: UpdateProfileInput!) {
  updateMyProfile(input: $input) {
    id
    fullName
    publicUsername
    displayName
    bio
    city
    isPublic
    followerCount
    followingCount
  }
}
