mutation CreateMaintenanceTask($input: CreateMaintenanceTaskInput!) {
  createMaintenanceTask(input: $input) {
    id
    title
    priority
    status
    dueDate
    targetMileage
    isRecurring
    intervalKm
    intervalDays
    createdAt
  }
}
