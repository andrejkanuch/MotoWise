mutation UpdateTrip($input: UpdateTripInput!) {
  updateTrip(input: $input) {
    id
    title
    description
    startDate
    endDate
    difficulty
    maxRiders
    status
    visibility
    waypoints {
      id
      tripId
      sortOrder
      dayIndex
      periodOfDay
      type
      name
      notes
      lat
      lng
    }
  }
}
