mutation MarkArticleRead($articleId: String!) {
  markArticleRead(articleId: $articleId) {
    id
    userId
    articleId
    articleRead
    quizCompleted
    quizBestScore
    firstReadAt
    lastReadAt
  }
}
