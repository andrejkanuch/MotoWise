query ExpensesByMotorcycle($motorcycleId: String!, $year: Int!) {
  expenses(motorcycleId: $motorcycleId, year: $year) {
    ytdTotal
    categories {
      category
      total
      expenses {
        id
        amount
        category
        currency
        description
        date
        createdAt
      }
    }
  }
}
