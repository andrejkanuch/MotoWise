query DiscoverTrips($filter: DiscoverTripsFilterInput, $first: Int, $after: String) {
  discoverTrips(filter: $filter, first: $first, after: $after) {
    edges {
      node {
        id
        slug
        title
        description
        difficulty
        dayCount
        startLat
        startLng
        countryCode
        regionCode
        distanceM
        elevationGainM
        surfaceType
        isFeatured
        isMotovaultPick
        viewCount
        cloneCount
        averageRating
        reviewCount
        publishedAt
        contributor {
          id
          displayName
          publicUsername
          avatarUrl
        }
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
