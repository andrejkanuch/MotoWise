# @deprecated — Use trip-based equivalent. Kept for backward compatibility with old app builds.
query RouteBySlugDeepLink($country: String!, $region: String!, $slug: String!) {
  routeBySlug(country: $country, region: $region, slug: $slug) {
    id
  }
}
