query OemSchedulesPreview($make: String!, $model: String, $year: Int) {
  oemSchedulesPreview(make: $make, model: $model, year: $year) {
    id
    taskName
    description
    intervalKm
    intervalDays
    priority
    sortOrder
  }
}
