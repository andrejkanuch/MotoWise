query OemSchedulesPreview($make: String!, $model: String, $year: Int, $variant: String) {
  oemSchedulesPreview(make: $make, model: $model, year: $year, variant: $variant) {
    id
    taskName
    description
    intervalKm
    intervalDays
    priority
    sortOrder
  }
}
